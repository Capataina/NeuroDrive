# Rust And Bevy Materials

Use this file as a topic guide for external study, not as a frozen bibliography.

## Study Topics

- Rust ownership and borrowing in ECS-heavy code
- Bevy plugins, schedules, resources, components, and queries
- Fixed timestep patterns in Bevy
- UI and gizmo debugging patterns in Bevy

## What To Connect Back To In NeuroDrive

- plugin composition in `src/main.rs`
- named fixed-update sets in `src/sim/sets.rs`
- resource-driven runtime state in `src/debug/` and `src/brain/`
- query shape and singleton assumptions in `src/game/`, `src/agent/`, and `src/debug/`
