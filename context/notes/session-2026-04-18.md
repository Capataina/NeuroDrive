# Session 18 April 2026

## What Was Done

Full `upkeep-context` pass under the upgraded skill (commit `3f37460` on `Capataina/agent-skills`), which introduced the "Six Mandatory Analytical Obligations" block. No source-code changes happened between the 2026-04-15 pass and this one — the git tree was clean of source edits before the pass, and this pass touches only `context/`.

The session focus was therefore **not** drift repair but deliberate execution of the analytical obligations that previous runs documented skipping (see `notes/session-2026-04-15.md`, Skill Execution Observations).

### Analytical work added to `context/`

- **Inter-System Relationships table** added to `architecture.md` — 13 canonical entries with mechanism, data, and failure mode. Canonical owner for major cross-system flows; `systems/*.md` files already describe interfaces at their ends.
- **Shared Infrastructure subsection** added to `architecture.md` documenting that `profiling` imports `RunContext` and `enforce_retention` from `analytics::exporters`. This was not previously surfaced as a structural dependency — the profiling feature would fail to compile if the analytics exporter module were ripped out or relocated.
- **Dependency chain trace** added to `architecture.md` — full 21-step PPO training tick from `frame_start_system` through `ppo_epoch_system` and `auto_exit_system`, with failure semantics at each ordering boundary. Verified against `src/main.rs`, plugin-registration output, and `systems/brain-ppo.md` claims.
- **Coverage (Knowledge Gaps) subsection** added to `architecture.md` — explicit enumeration of what was directly inspected, what was read via the scan tool only, and what was trusted against existing context without re-verification.
- **`notes/conventions.md`** created — nine codebase conventions captured (per-car Component discipline, shared RNG seeding pattern, `*Config` struct naming, no Bevy events, `debug_assert!` usage, compile-time feature gating, export directory layout + retention, shared `RunContext` struct, plugin-scoped system registration).
- **`notes.md`** index updated with new entries.

### Verification items resolved

- **`unsafe` in `src/brain/ppo/update.rs`** (flagged by 2026-04-15 session): inspected at lines 159-162 and 289-292. Both blocks are read-only aliasing of scratch buffers (`obs_batch`, `grad_seed_values`, `grad_seed_means`) that are disjoint from the `&mut` scratch fields used during `forward_batch`/`backward_batch`. SAFETY comments are accurate. This is a borrow-checker workaround, not an unsound pattern. The follow-up item from the 2026-04-15 note can be closed.

## Items Not Addressed

- **`context/plans/code-health-audit/` working-tree state** — when this session began, the audit plan folder had been reorganised (its six original files moved into `archive-2026-04-15/` and an `obligation-evidence-map.md` added), but those moves are uncommitted and appear to belong to a separate skill run (`code-health-audit`) still in flight. This upkeep pass did **not** touch that folder or those uncommitted changes. The plan folder should be revisited by whichever skill owns it; touching it from `upkeep-context` would cross scope.
- **Performance reality check** (carried over from 2026-04-15) — still pending; requires running `cargo run --features profiling` against the current binary, which is outside an upkeep pass's scope.
- **User-controllable PPO init seed** — still open; `systems/determinism.md` documents this correctly and `notes/conventions.md` now names the two-site seeding pattern that would need updating.

## Skill Execution Observations

Unlike the 2026-04-15 run, this pass did execute the six analytical obligations rather than skipping them. The tool-bias failure mode described in `SKILL.md` ("I have enough context already" / "this is obvious from the code") was active at several moments — most notably when drafting the dependency chain trace, where it was tempting to paraphrase `systems/brain-ppo.md` instead of re-reading `src/main.rs` and confirming plugin order. The corrective move was to grep for `struct PpoConfig|struct TrainerConfig|...` and verify each canonical config site in code rather than relying on docstring memory.

The pre-commitment skeleton in the transcript forced the obligations into attention before writing content, which is likely what made the difference.

## Evidence Summary

- `scripts/scan_repo.py` ran at start of session — 2015 lines of output, 67 Rust files inventoried.
- `scripts/lint_context.py` ran at end of session — disposition for each warning recorded in final handoff.
- Grep passes executed for rationale capture (`WHY|NOTE|HACK|IMPORTANT|TODO|FIXME|SAFETY`), for convention capture (`EventWriter|EventReader`, `StdRng|rand::rng`, `debug_assert`, `pub const`, `#[cfg(feature`), and for shared data structures (`struct *Config`, `RunContext`, `EnvInstanceId`).
- Direct file reads: `src/main.rs`, `src/brain/ppo/update.rs` (lines 140-300), `src/brain/ppo/mod.rs` (lines 25-80), `src/analytics/exporters/context.rs` (header), `src/analytics/exporters/cleanup.rs` (full).

## Commits

None yet — the user asked this session not to commit. The following files are modified / new and can be reviewed:

- `context/architecture.md` — added Inter-System Relationships, Shared Infrastructure, Dependency Chain Trace, Coverage sections.
- `context/notes.md` — index updated.
- `context/notes/conventions.md` — new.
- `context/notes/session-2026-04-18.md` — this file.
